name: MatchQuest
description: ""
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.32.0
  firebase_auth: ^4.17.5
  google_sign_in: ^6.2.1
  cupertino_icons: ^1.0.8
  provider: ^6.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.13.1 # ✅ 아이콘 생성용 패키지 추가

flutter_icons: # ✅ 아이콘 설정
  android: true
  ios: true
  image_path: "assets/icon/main.png"

flutter:
  uses-material-design: true

  assets:
    - assets/icon/main.png # ✅ 아이콘 이미지 경로 등록
